<h1>Tags</h1>

<p>
	<% @tags.each do |tag| %>
		<%= link_to tag.name, tag_path(tag) %> - <%= link_to "delete", tag_path(tag), method: :delete, data: {confirm: "Really delete the tag?"} %>, 
	<% end %>
</p>